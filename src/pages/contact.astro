---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navigation from '../components/Navigation.astro';
import Hero from '../components/Hero.astro';

const students = [
  { name: 'Emma Johnson', role: 'roles.projectLead' },
  { name: 'Lucas Martinez', role: 'roles.developer' },
  { name: 'Sophia Anderson', role: 'roles.designer' },
  { name: 'Oliver Brown', role: 'roles.contentWriter' },
  { name: 'Ava Williams', role: 'roles.researcher' },
];
---

<BaseLayout title="Contact Us - Doeltreffend">
  <Navigation />
  
  <Hero 
    title="Contact Us" 
    subtitle="Get in touch with our team"
  />
  
  <section class="py-24 px-6">
    <div class="max-w-6xl mx-auto">
      <!-- Our Team Section -->
      <div class="mb-20">
        <h2 class="text-4xl font-serif font-bold text-center mb-4" data-i18n="contact.ourTeam">Our Team</h2>
        <p class="text-center text-white/70 mb-12" data-i18n="contact.teamDescription">Meet the students who made this project possible</p>
        
        <div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-5 gap-8">
          {students.map((student) => (
            <div class="text-center group">
              <div class="relative mb-4 overflow-hidden rounded-full w-32 h-32 mx-auto bg-gradient-to-br from-amber-500 to-amber-700 flex items-center justify-center shadow-lg transition-transform duration-300 group-hover:scale-105">
                <!-- Placeholder avatar with initials -->
                <span class="text-4xl font-bold text-white">
                  {student.name.split(' ').map(n => n[0]).join('')}
                </span>
              </div>
              <h3 class="text-lg font-semibold mb-1">{student.name}</h3>
              <p class="text-sm text-white/60" data-i18n={student.role}>{ student.role.split('.')[1] }</p>
            </div>
          ))}
        </div>
      </div>

      <!-- Contact Form Section -->
      <div class="max-w-2xl mx-auto">
        <div class="bg-white/5 backdrop-blur-sm rounded-2xl p-8 border border-white/10">
          <h2 class="text-3xl font-serif font-bold mb-2 text-center" data-i18n="contact.sendMessage">Send Us a Message</h2>
          <p class="text-white/70 mb-8 text-center" data-i18n="contact.formDescription">Have a question or want to work together?</p>
          
          <form id="contact-form" class="space-y-6">
            <div>
              <label for="name" class="block text-sm font-medium mb-2" data-i18n="contact.name">Name</label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-lg focus:outline-none focus:border-amber-500 focus:ring-1 focus:ring-amber-500 transition-colors"
                data-i18n-placeholder="contact.namePlaceholder"
                placeholder="Your name"
              />
            </div>

            <div>
              <label for="email" class="block text-sm font-medium mb-2" data-i18n="contact.email">Email</label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-lg focus:outline-none focus:border-amber-500 focus:ring-1 focus:ring-amber-500 transition-colors"
                data-i18n-placeholder="contact.emailPlaceholder"
                placeholder="your.email@example.com"
              />
            </div>

            <div>
              <label for="subject" class="block text-sm font-medium mb-2" data-i18n="contact.subject">Subject</label>
              <input
                type="text"
                id="subject"
                name="subject"
                required
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-lg focus:outline-none focus:border-amber-500 focus:ring-1 focus:ring-amber-500 transition-colors"
                data-i18n-placeholder="contact.subjectPlaceholder"
                placeholder="What is this about?"
              />
            </div>

            <div>
              <label for="message" class="block text-sm font-medium mb-2" data-i18n="contact.message">Message</label>
              <textarea
                id="message"
                name="message"
                required
                rows="6"
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-lg focus:outline-none focus:border-amber-500 focus:ring-1 focus:ring-amber-500 transition-colors resize-none"
                data-i18n-placeholder="contact.messagePlaceholder"
                placeholder="Tell us more..."
              ></textarea>
            </div>

            <div id="form-message" class="hidden p-4 rounded-lg"></div>

            <button
              type="submit"
              id="submit-button"
              class="w-full bg-amber-600 hover:bg-amber-700 text-white font-semibold py-3 px-6 rounded-lg transition-colors duration-300 disabled:opacity-50 disabled:cursor-not-allowed"
              data-i18n="contact.sendButton"
            >
              Send Message
            </button>
          </form>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  import { getPreferredLanguage, t } from '../i18n/utils';
  
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const submitButton = document.getElementById('submit-button') as HTMLButtonElement;
  const formMessage = document.getElementById('form-message') as HTMLDivElement;

  // Update all translations on the page
  function updateTranslations() {
    const currentLang = getPreferredLanguage();
    
    // Update text content
    document.querySelectorAll('[data-i18n]').forEach((el) => {
      const key = (el as HTMLElement).dataset.i18n;
      if (key) {
        el.textContent = t(key, currentLang);
      }
    });
    
    // Update placeholders
    document.querySelectorAll('[data-i18n-placeholder]').forEach((el) => {
      const key = (el as HTMLElement).dataset.i18nPlaceholder;
      if (key) {
        (el as HTMLInputElement).placeholder = t(key, currentLang);
      }
    });
    
    // Update Hero title and subtitle
    const heroTitle = document.querySelector('.hero-section h1');
    const heroSubtitle = document.querySelector('.hero-section p');
    if (heroTitle) heroTitle.textContent = t('contact.title', currentLang);
    if (heroSubtitle) heroSubtitle.textContent = t('contact.subtitle', currentLang);
  }
  
  // Initialize translations
  updateTranslations();

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const currentLang = getPreferredLanguage();
    
    // Get form data
    const formData = new FormData(form);
    const data = {
      name: formData.get('name'),
      email: formData.get('email'),
      subject: formData.get('subject'),
      message: formData.get('message'),
    };

    // Disable submit button
    submitButton.disabled = true;
    submitButton.textContent = t('contact.sending', currentLang);
    
    // Hide previous message
    formMessage.classList.add('hidden');

    try {
      const response = await fetch('/api/contact', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify(data),
      });

      const result = await response.json();

      if (response.ok) {
        // Success
        formMessage.className = 'p-4 rounded-lg bg-green-500/20 border border-green-500/50 text-green-300';
        formMessage.textContent = t('contact.successMessage', currentLang);
        formMessage.classList.remove('hidden');
        form.reset();
      } else {
        // Error
        formMessage.className = 'p-4 rounded-lg bg-red-500/20 border border-red-500/50 text-red-300';
        formMessage.textContent = result.error || t('contact.errorMessage', currentLang);
        formMessage.classList.remove('hidden');
      }
    } catch (error) {
      console.error('Form submission error:', error);
      formMessage.className = 'p-4 rounded-lg bg-red-500/20 border border-red-500/50 text-red-300';
      formMessage.textContent = t('contact.genericError', currentLang);
      formMessage.classList.remove('hidden');
    } finally {
      // Re-enable submit button
      submitButton.disabled = false;
      submitButton.textContent = t('contact.sendButton', currentLang);
    }
  });
</script>
